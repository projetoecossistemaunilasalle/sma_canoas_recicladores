services:
  db:
    build:
      context: ./docker/postgres
      dockerfile: Dockerfile
    container_name: sma-canoas-db
    environment:
      POSTGRES_DB: recicladores
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d recicladores"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  #backend:
  #  build:
  #    context: ./backend
  #    dockerfile: Dockerfile
  #  container_name: sma-canoas-backend
  #  environment:
  #    NODE_ENV: production
  #    PORT: 3001
  #    DATABASE_URL: postgresql://postgres:postgres@db:5432/recicladores?sslmode=disable
  #  depends_on:
  #    db:
  #      condition: service_healthy
  #  ports:
  #    - "3001:3001"
#
  #frontend:
  #  build:
  #    context: ./frontend
  #    dockerfile: Dockerfile
  #  container_name: sma-canoas-frontend
  #  environment:
  #    NODE_ENV: production
  #    PORT: 3000
  #    NEXT_PUBLIC_API_URL: http://backend:3001
  #  depends_on:
  #    - backend
  #  ports:
  #    - "3000:3000"
#
volumes:
  postgres_data:
